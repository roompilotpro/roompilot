### /commit-merge-push
Commit, merge into main, push both branches
```
Look through all the current changes, commit and push with a proper commit message. Then merge current branch into main. Finally push both branches
```